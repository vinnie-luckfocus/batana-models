---
name: environment-setup-and-rtmpose-download
status: backlog
created: 2026-04-13T00:50:16Z
---

# Task: Environment Setup and RTMPose-m Weight Download

## Description
Initialize the project directory structure, lock Python dependencies in `requirements.txt`, install all packages, and download the RTMPose-m COCO pretrained weights to `models/2d_pose/rtmpose_m_coco.pth` with SHA-256 verification. Profile inference latency on 1000 frames to confirm RTMPose-m runs under 35 ms/frame on the target GPU; trigger fallback planning if exceeded.

## Acceptance Criteria
- [ ] Directory tree created: `data/raw_videos/train/`, `data/raw_videos/val/`, `models/`, `configs/`, `scripts/`, `logs/`, `reports/`, `visuals/`, `data/train_2d_keypoints/`, `data/val_2d_keypoints/`, `data/train_2d_cleaned/`, `data/val_2d_cleaned/`
- [ ] `requirements.txt` written with pinned versions for `torch`, `torchvision`, `mmpose`, `mmcv-lite`, `mmdet`, `opencv-python-headless`, `numpy`, `scipy`, `scikit-image`, `pandas`, `tqdm`, `pyyaml`
- [ ] Verify raw video directories exist and contain expected `.mp4`/`.mov` files; log file count
- [ ] `models/2d_pose/rtmpose_m_coco.pth` downloaded and SHA-256 verified
- [ ] Latency profiling script runs 1000 frames and logs average ms/frame; fallback flag created if > 35 ms
- [ ] `configs/keypoint_schema.yaml` created defining the 19 keypoints (COCO-17 + bat_knob + bat_barrel)
